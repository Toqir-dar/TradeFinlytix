Design System

Simple shared design system primitives used across the frontend:

- Button
- Card
- Typography (H1, H2, P)
- theme tokens

Usage:

import { Button, Card, Typography } from "@/design-system";
